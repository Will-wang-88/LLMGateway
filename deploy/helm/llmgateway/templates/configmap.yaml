apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "llmgateway.fullname" . }}-config
  labels:
    {{- include "llmgateway.labels" . | nindent 4 }}
data:
  gateway.yaml: |
{{ .Values.config | indent 4 }}
