apiVersion: v1
kind: Service
metadata:
  name: {{ include "llmgateway.fullname" . }}
  labels:
    {{- include "llmgateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "llmgateway.selectorLabels" . | nindent 4 }}
